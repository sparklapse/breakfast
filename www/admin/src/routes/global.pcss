@tailwind base;
@tailwind components;
@tailwind utilities;

@layer {
  .breakfast-input {
    @apply rounded border;
  }
}

body {
  @apply text-slate-900;
  font-family: "Gabarito Variable", system-ui;
}
